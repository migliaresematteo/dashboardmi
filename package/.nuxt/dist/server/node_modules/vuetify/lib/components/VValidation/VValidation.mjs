import { makeValidationProps, useValidation } from "../../composables/validation.mjs";
import { genericComponent } from "../../util/defineComponent.mjs";
const VValidation = genericComponent()({
  name: "VValidation",
  props: makeValidationProps(),
  emits: {
    "update:modelValue": (value) => true
  },
  setup(props, _ref) {
    let {
      slots
    } = _ref;
    const validation = useValidation(props, "validation");
    return () => {
      var _a;
      return (_a = slots.default) == null ? void 0 : _a.call(slots, validation);
    };
  }
});
export {
  VValidation
};
//# sourceMappingURL=VValidation.mjs.map
